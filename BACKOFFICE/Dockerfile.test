FROM node:18

WORKDIR /app
COPY . .

RUN npm install --legacy-peer-deps
RUN npm run build

# Lance vite preview sur le port par défaut
EXPOSE 4173
CMD ["npx", "vite", "preview", "--host"]
